# Browser Tool Source

This directory contains the TypeScript source code for the browser automation tool.

## Files

- `index.ts`: The main entry point script. It uses Playwright (`chromium`) to launch a browser instance, navigate to a target URL (e.g., Hacker News), and extract information such as the page title, links, and HTML content.
