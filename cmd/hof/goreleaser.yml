project_name: hof
release:
  disable: false
  draft: true
  github:
    name: hof
    owner: hofstadter-io
changelog:
  filters:
    exclude:
      - '^docs:'
      - '^test:'
  sort: asc
checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_checksums.txt'
snapshot:
  name_template: '{{ .Tag }}-SNAPSHOT-{{ .ShortCommit }}'
builds:
  - binary: hof
    env:
      - CGO_ENABLED=0
    goarch:
      - amd64
      - arm64
    goos:
      - darwin
      - linux
      - windows
    ldflags:
      - -s -w
      - -X github.com/hofstadter-io/hof/cmd/hof/verinfo.Version={{ .Version }}
      - -X github.com/hofstadter-io/hof/cmd/hof/verinfo.Commit={{ .FullCommit }}
      - -X github.com/hofstadter-io/hof/cmd/hof/verinfo.BuildDate={{ .Date }}
      - -X github.com/hofstadter-io/hof/cmd/hof/verinfo.BuildOS={{ .Os }}
      - -X github.com/hofstadter-io/hof/cmd/hof/verinfo.BuildArch={{ .Arch }}
      - -X github.com/hofstadter-io/hof/cmd/hof/verinfo.BuildArm={{ .Arm }}
    main: main.go
dockers:
  - dockerfile: ../../ci/hof/docker/Dockerfile.debian
    skip_push: false
    image_templates:
      - hofstadter/hof:{{.Tag}}
      - hofstadter/hof:{{ .ShortCommit }}
      - hofstadter/hof:latest
  - dockerfile: ../../ci/hof/docker/Dockerfile.debian
    skip_push: false
    image_templates:
      - hofstadter/hof:debian-{{.Tag}}
      - hofstadter/hof:debian-{{ .ShortCommit }}
      - hofstadter/hof:debian-latest
  - dockerfile: ../../ci/hof/docker/Dockerfile.alpine
    skip_push: false
    image_templates:
      - hofstadter/hof:alpine-{{.Tag}}
      - hofstadter/hof:alpine-{{ .ShortCommit }}
      - hofstadter/hof:alpine-latest
archives:
  - files:
      - thisfiledoesnotexist*
    format: binary
    replacements:
      amd64: x86_64
      darwin: Darwin
      linux: Linux
      windows: Windows
