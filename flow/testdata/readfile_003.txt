! exec hof flow flow.cue
cmp stdout golden.stdout

-- flow.cue --
tasks: {
  @flow(readfile)
	r: { f: "in.txt", contents: string } @task(os.ReadFile)
}

-- golden.stdout --
Error: task failed: open in.txt: no such file or directory

