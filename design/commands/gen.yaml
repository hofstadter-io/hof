cli:
  name: hof

  commands:

    - name: gen
      usage: "gen <entrypoint>"
      aliases:
        - g
      short: Generate a project.
      long: Generate a project starting from the file.

      args:
        - name: entrypoint
          type: "string"
          default: "."

