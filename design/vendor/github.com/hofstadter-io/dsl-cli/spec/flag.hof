package cli

Flag : {
  type:        any
  default:     builtin
  help:        string
  long:        string
  short:       string
  validation:  Validation
}

NewFlag <- (typ any) & Flag {
  type: typ
  validation: Validation(typ)
}
