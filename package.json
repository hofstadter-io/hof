{
  "name": "hof",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "vscode:build": "pnpm run --sort --recursive --stream '/^(build)$/'",
    "vscode:build:prod": "pnpm run --sort --recursive --stream '/^(build:prod)$/'",
    "vscode:package": "pnpm run --sort --recursive --stream '/^(vscode:package)$/'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.18.3"
}
